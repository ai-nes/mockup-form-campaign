import type { TaskManagementItem } from "./types";

/**
 * No live API for task management yet — keep this empty so the page
 * renders its standard empty state instead of fabricated rows.
 */
export const taskManagementData: TaskManagementItem[] = [];
